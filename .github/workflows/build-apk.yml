name: Build Android APK (Patalix)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Set up Java 17
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Install Cordova and Setup Project
      run: |
        npm install -g cordova
        cordova create myApp com.patalix.calc "Patalix Calc"
        rm -rf myApp/www/*
        cp calculator.html myApp/www/index.html

    - name: Setup Android SDK
      uses: android-actions/setup-android@v4

    - name: Add Android Platform and Build APK
      run: |
        cd myApp
        cordova platform add android
        cordova build android

    - name: Upload APK to GitHub Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: Patalix-Calculator-APK
        path: myApp/platforms/android/app/build/outputs/apk/**/*.apk
